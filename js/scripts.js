// Utility Logic
window.onload = function() {
  let form = document.querySelector("form");
  form.onsubmit = function(event) {
  event.preventDefault();
  };
};

// Business Logic

let cCValidator = (number) {
  for (let i = 0; i <= number.length; i++) {
    if number[i] === 
  }
}

